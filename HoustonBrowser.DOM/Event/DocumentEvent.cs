using System;

namespace HoustonBrowser.DOM
{
    class DocumentEvent
    {
        public Event CreateEvent(string eventType)
        {
            Event current = new Event();
            return current;
        }
    }
}